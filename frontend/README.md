# Frontend

React 19 + TypeScript + Vite + Tailwind CSS 4, with Monaco as the code editor.

```bash
npm install
npm run dev      # dev server on :3000, proxies /api to the backend on :8000
npm run build    # production bundle in dist/, served by FastAPI
```
